- name: Start a windows service
  ansible.windows.win_service:
  vars:
    service_name: 'GlassFish4'
    service_state: 'started'
  
  tasks:
    name: "{{ service_name }}"
    state: "{{ service_state }}"
